# Data Model

This document describes the core data model for the Retail Sales Warehouse.

Entities:

- `sales` — transactional sales data (order_id, customer_id, product_id, order_date, amount)
- `customer` — customer dimension with SCD handling
- `product` — product dimension

Populate this file with attribute lists, keys, and relationships.
